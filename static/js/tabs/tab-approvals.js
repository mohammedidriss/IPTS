// Approvals tab functions are defined in tab-admin.js:
// - loadApprovals() — card-style HITL queue view (used by approvals tab)
// - hitlAction()    — approve/reject HITL items
// - loadHITL()     — table-style HITL queue view (used by admin tab)
// This file is intentionally minimal; loadApprovals is the entry point called by switchTab('approvals').
